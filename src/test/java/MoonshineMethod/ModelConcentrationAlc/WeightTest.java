package MoonshineMethod.ModelConcentrationAlc;

import MoonshineData.DataConcentrationAlc.AlcConcentration;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

class WeightTest {
/*
    @Test
    void weightConcentration() {
        AlcConcentration alcConcentration  = new AlcConcentration();
        alcConcentration.setAlcWeightConcentration (1.0);
        alcConcentration.setDensity ( 1.0 );
        alcConcentration.setAlcConcentration ( 1.0 );
        alcConcentration.setTemperature (1.0);
        alcConcentration.setPureAlcDensity ( 1.0 );
        alcConcentration.setAlcVolumeConcentration ( 1.0 );
        alcConcentration.getPureAlcWeightConcentration ();
        Weight weight = mock(Weight.class);
        when(weight.weightConcentration (alcConcentration)).thenReturn(alcConcentration);


        assertEquals("Susan Ivanova",fullName);
    }

 */
}